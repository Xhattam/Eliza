import time

def timeout_talk(): prend un élément au hasard dans la liste, a appeler avec timeout, qui va calculer le temps passé depuis la réponse d'Eliza
    lst = ["Ne voulez-vous pas m'en dire plus?" , "Etes-vous toujours là?" , "Si vous souhaitez de l'aide, il faut       parler!" , "Allons, dites-m'en encore un peu!"]
   	i = random.randint(0, len(lst) - 1)  
   	elem = lst[i] 
        
            
def wait():
	time.sleep(2) #temps d'attente: deux secondes (reflexion)
	print "Hello" #Ici, au lieu de Hello, on aura la réponse d'Eliza
  
